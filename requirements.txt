# Core RAG stack
# Version ranges (>=) rather than exact pins, so pip picks a compatible build
# for your Python version instead of failing on a single unavailable version.
langchain>=0.2.16
langchain-community>=0.2.16
langchain-huggingface>=0.0.3
langchain-text-splitters>=0.2.0
faiss-cpu>=1.8.0
sentence-transformers>=3.0.0
pypdf>=4.3.0

# Local LLM (free, private) via Ollama
langchain-ollama>=0.1.3

# Demo interface
streamlit>=1.37.0
